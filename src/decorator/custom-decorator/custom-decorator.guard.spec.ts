import { CustomDecoratorGuard } from './custom-decorator.guard';

describe('CustomDecoratorGuard', () => {
  it('should be defined', () => {
    expect(new CustomDecoratorGuard()).toBeDefined();
  });
});
